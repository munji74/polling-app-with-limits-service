package com.microservices.userservice.repository;

// Deprecated: roles are stored as enum values in user_roles; no RoleRepository required.
@Deprecated
public interface RoleRepository {
    // intentionally empty
}
